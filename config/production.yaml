build:
  configuration: Release
  metadata:
    pre:

deployment:
  canoncial_url: http://watch.grossweber.com/

client:
  log:
    level: Warn

web:
  bindings:
    -
      :protocol: http
      :host: watch.grossweber.com
