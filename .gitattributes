# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Files that will always have CRLF line endings on checkout.
*.cmd text eol=crlf
# Shell scripts requre LF.
*.sh text eol=lf

# Ruby.
*.rb diff=ruby
*.rake diff=ruby

# Visual Studio.
*.sln text eol=crlf merge=union
*.csproj merge=union
*.cs diff=csharp

